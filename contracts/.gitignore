# Foundry build artifacts (regenerated by `forge build`)
out/
cache/

# Secrets — never commit
.env
.env.local

# Node
node_modules/

# Keep broadcast/ IN repo — deploy artifacts are proof of testnet deployment.
